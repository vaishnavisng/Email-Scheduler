# Demo overrides: tiny rate-limit window and low per-sender cap so the
# rate-limiter and Slack notification are observable in a short live demo.
#   docker compose --env-file .env.demo up
# Inherits everything else from .env.example defaults.

RATE_LIMIT_WINDOW_MS=120000
MAX_EMAILS_PER_HOUR_PER_SENDER=5
